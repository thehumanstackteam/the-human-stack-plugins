---
description: Batch-sweep local Claude Code session transcripts to Supabase. Scope options -- current repo (default), all repos, or by date.
argument-hint: "[all | YYYY-MM-DD]"
---

Route this request through the ingest skill at `${CLAUDE_PLUGIN_ROOT}/skills/ingest/SKILL.md`.

Pass the user's argument (if any) as the scope parameter.
